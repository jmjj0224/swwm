# SWWM 약속 캘린더

여러 사용자가 비는 시간을 공유하고 최적의 약속 시간을 실시간으로 찾는 모바일 특화 웹 애플리케이션입니다.

## 주요 기능

- 🏠 **방 시스템**: 6자리 코드로 간편하게 방 생성/입장
- 👤 **다중 프로필 관리**: 한 기기에서 여러 프로필 관리 가능 (친구 대신 입력 가능)
- 📅 **직관적인 시간 선택**: 1시간 단위, 범위 선택, 하루 종일 선택 지원
- ⚡ **실시간 동기화**: Supabase Realtime으로 모든 변경사항 즉시 반영
- ✅ **최적 시간 제안**: 겹치는 날짜를 자동으로 찾아 추천 (전원 가능: 초록색, 일부 가능: 노란색)
- 🎨 **모바일 최적화**: Vaul drawer를 활용한 터치 친화적 UX

## 기술 스택

- **Frontend**: React 19 + Next.js 16 + TypeScript
- **Styling**: Tailwind CSS 3
- **UI Components**: shadcn/ui (Radix UI 기반)
- **Backend**: Supabase (PostgreSQL + Realtime)
- **State Management**: Zustand
- **Date Handling**: date-fns
- **Mobile UX**: Vaul (drawer/bottom sheet)

## 시작하기

### 1. 환경 변수 설정

`.env.local` 파일을 생성하고 다음 내용을 입력하세요:

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Google Gemini API (선택)
NEXT_PUBLIC_GEMINI_API_KEY=your_gemini_api_key
```

### 2. 의존성 설치

```bash
npm install
```

### 3. Supabase 마이그레이션 실행

Supabase 대시보드에서 다음 마이그레이션 파일들을 순서대로 실행하세요:

```
supabase/migrations/
├── 001_create_rooms.sql
├── 002_create_room_users.sql
├── 003_create_time_selections.sql
├── 004_enable_realtime.sql
├── 005_add_user_fields.sql
└── 006_add_delete_policies.sql
```

### 4. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 [http://localhost:3000](http://localhost:3000)을 열어 확인하세요.

## 프로젝트 구조

```
swwm-api-keys/
├── src/
│   ├── app/                      # Next.js App Router
│   │   ├── page.tsx              # 메인 페이지 (방 생성/입장)
│   │   └── room/[code]/          # 방 페이지
│   ├── components/
│   │   ├── calendar/             # 캘린더 관련 컴포넌트
│   │   ├── room/                 # 방/사용자 관리
│   │   ├── suggestions/          # 최적 시간 제안
│   │   └── ui/                   # shadcn/ui 기본 컴포넌트
│   ├── lib/
│   │   ├── algorithms/           # 겹침 계산 알고리즘
│   │   ├── calendar/             # 날짜 계산 유틸
│   │   ├── stores/               # Zustand 상태 관리
│   │   └── supabase/             # Supabase 클라이언트
│   └── types/                    # TypeScript 타입 정의
└── supabase/
    └── migrations/               # 데이터베이스 마이그레이션
```

## 주요 알고리즘

### 겹치는 시간 계산 (`src/lib/algorithms/overlap-calculator.ts`)

1. 사용자별 시간 선택을 1시간 단위로 분해
2. 같은 날짜/시간대에 2명 이상이 선택한 경우 겹침으로 판단
3. 전원이 선택한 경우 `isFullOverlap = true`로 표시
4. 점수 계산: `(참석자 수 / 전체 인원) * 100 + (전원 보너스)`

### 날짜별 그룹핑 (`src/components/suggestions/best-time-suggestions.tsx`)

- 시간대별로 흩어진 결과를 날짜별로 묶어 표시
- 각 날짜의 최고 점수를 기준으로 정렬
- 날짜 클릭 시 시간대별 상세 정보 표시

## 배포

### Vercel 배포 (권장)

```bash
# Vercel CLI 설치
npm i -g vercel

# 배포
vercel
```

환경 변수를 Vercel 대시보드에서 설정하세요.

## 라이선스

MIT

## 기여

이슈와 풀 리퀘스트는 언제나 환영합니다!
